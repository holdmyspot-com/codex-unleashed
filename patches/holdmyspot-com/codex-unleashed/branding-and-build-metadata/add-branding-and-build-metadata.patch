From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Codex Unleashed <holdmyspot-com@users.noreply.github.com>
Date: Sun, 6 Sep 2026 00:00:00 -0400
Subject: [PATCH] Identify Codex Unleashed provider builds

---
 codex-rs/cli/build.rs                    |  5 +++++
 codex-rs/cli/src/main.rs                 | 34 ++++++++++++++++++++++++++++++++++-
 codex-rs/tui/Cargo.toml                  |  1 +
 codex-rs/tui/build.rs                    |  7 +++++++
 codex-rs/tui/src/history_cell/session.rs | 11 ++++++++++-
 codex-rs/tui/src/version.rs              |  8 +++++++-
 5 files changed, 55 insertions(+), 3 deletions(-)

diff --git a/codex-rs/cli/build.rs b/codex-rs/cli/build.rs
index abccc48..b5b80c1 100644
--- a/codex-rs/cli/build.rs
+++ b/codex-rs/cli/build.rs
@@ -1,4 +1,9 @@
 fn main() {
+    let build_number =
+        std::env::var("CODEX_UNLEASHED_BUILD_NUMBER").unwrap_or_else(|_| "dev".to_string());
+    println!("cargo:rustc-env=CODEX_UNLEASHED_BUILD_NUMBER={build_number}");
+    println!("cargo:rerun-if-env-changed=CODEX_UNLEASHED_BUILD_NUMBER");
+
     if std::env::var("CARGO_CFG_TARGET_OS").as_deref() == Ok("macos") {
         println!("cargo:rustc-link-arg=-ObjC");
     }
diff --git a/codex-rs/cli/src/main.rs b/codex-rs/cli/src/main.rs
index ee42ceb..a4a3ddd 100644
--- a/codex-rs/cli/src/main.rs
+++ b/codex-rs/cli/src/main.rs
@@ -114,10 +114,33 @@ use codex_terminal_detection::TerminalName;
 /// Codex CLI
 ///
 /// If no subcommand is specified, options will be forwarded to the interactive CLI.
+const CLI_VERSION: &str = concat!(
+    env!("CARGO_PKG_VERSION"),
+    "+",
+    env!("CODEX_UNLEASHED_BUILD_NUMBER"),
+    "\n(See --build-info for more information)"
+);
+
+const BUILD_INFO: &str = concat!(
+    "Codex Unleashed build information\n\n",
+    "Codex CLI version: ",
+    env!("CARGO_PKG_VERSION"),
+    "+",
+    env!("CODEX_UNLEASHED_BUILD_NUMBER"),
+    "\nBuild number:      ",
+    env!("CODEX_UNLEASHED_BUILD_NUMBER"),
+    "\n\nUpstream project:  OpenAI Codex\n",
+    "Upstream version:  ",
+    env!("CARGO_PKG_VERSION"),
+    "\nUpstream source:   https://github.com/openai/codex\n",
+    "\nProvided by:       Codex Unleashed\n",
+    "Project:           https://github.com/holdmyspot-com/codex-unleashed"
+);
+
 #[derive(Debug, Parser)]
 #[clap(
     author,
-    version,
+    version = CLI_VERSION,
     // If a sub‑command is given, ignore requirements of the default args.
     subcommand_negates_reqs = true,
     // The executable is sometimes invoked via a platform‑specific name like
@@ -127,6 +150,10 @@ use codex_terminal_detection::TerminalName;
     override_usage = "codex [OPTIONS] [PROMPT]\n       codex [OPTIONS] <COMMAND> [ARGS]"
 )]
 struct MultitoolCli {
+    /// Print information about the provider build and its upstream source.
+    #[clap(long)]
+    build_info: bool,
+
     #[clap(flatten)]
     pub config_overrides: CliConfigOverrides,
 
@@ -1136,12 +1163,17 @@ async fn cli_main(
 ) -> anyhow::Result<()> {
     let MultitoolCli {
+        build_info,
         config_overrides: mut root_config_overrides,
         feature_toggles,
         remote,
         mut interactive,
         subcommand,
     } = MultitoolCli::parse();
+    if build_info {
+        println!("{BUILD_INFO}");
+        return Ok(());
+    }
     reject_unsupported_worktree_for_subcommand(interactive.shared.worktree, &subcommand)?;
     // Fold --enable/--disable into config overrides so they flow to all subcommands.
     let toggle_overrides = feature_toggles.to_overrides()?;
     root_config_overrides.raw_overrides.extend(toggle_overrides);
diff --git a/codex-rs/tui/Cargo.toml b/codex-rs/tui/Cargo.toml
index f72429a..d79114e 100644
--- a/codex-rs/tui/Cargo.toml
+++ b/codex-rs/tui/Cargo.toml
@@ -4,6 +4,7 @@ version.workspace = true
 edition.workspace = true
 license.workspace = true
 autobins = false
+build = "build.rs"
 
 [[bin]]
 name = "codex-tui"
 path = "src/main.rs"
diff --git a/codex-rs/tui/build.rs b/codex-rs/tui/build.rs
new file mode 100644
index 0000000..a2e5b86
--- /dev/null
+++ b/codex-rs/tui/build.rs
@@ -0,0 +1,6 @@
+fn main() {
+    let build_number =
+        std::env::var("CODEX_UNLEASHED_BUILD_NUMBER").unwrap_or_else(|_| "dev".to_string());
+    println!("cargo:rustc-env=CODEX_UNLEASHED_BUILD_NUMBER={build_number}");
+    println!("cargo:rerun-if-env-changed=CODEX_UNLEASHED_BUILD_NUMBER");
+}
diff --git a/codex-rs/tui/src/version.rs b/codex-rs/tui/src/version.rs
index 8c8d108..516d07d 100644
--- a/codex-rs/tui/src/version.rs
+++ b/codex-rs/tui/src/version.rs
@@ -1,2 +1,8 @@
 /// The current Codex CLI version as embedded at compile time.
-pub const CODEX_CLI_VERSION: &str = env!("CARGO_PKG_VERSION");
+pub const CODEX_CLI_VERSION: &str = concat!(
+    env!("CARGO_PKG_VERSION"),
+    "+",
+    env!("CODEX_UNLEASHED_BUILD_NUMBER")
+);
+
+pub const CODEX_UNLEASHED_URL: &str = "https://holdmyspot.com/codex-unleashed/";
-- 
2.43.0
