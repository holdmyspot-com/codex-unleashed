diff --git a/codex-rs/cli/src/main.rs b/codex-rs/cli/src/main.rs
--- a/codex-rs/cli/src/main.rs
+++ b/codex-rs/cli/src/main.rs
@@ -98,6 +98,13 @@
 /// Codex CLI
 ///
 /// If no subcommand is specified, options will be forwarded to the interactive CLI.
+#[cfg(test)]
+const CLI_VERSION: &str = concat!(
+    env!("CARGO_PKG_VERSION"),
+    "\n(See --build-info for more information)"
+);
+
+#[cfg(not(test))]
 const CLI_VERSION: &str = concat!(
     env!("CARGO_PKG_VERSION"),
     "+",
diff --git a/codex-rs/tui/src/version.rs b/codex-rs/tui/src/version.rs
--- a/codex-rs/tui/src/version.rs
+++ b/codex-rs/tui/src/version.rs
@@ -1,4 +1,8 @@
 /// The current Codex CLI version as embedded at compile time.
+#[cfg(test)]
+pub const CODEX_CLI_VERSION: &str = env!("CARGO_PKG_VERSION");
+
+#[cfg(not(test))]
 pub const CODEX_CLI_VERSION: &str = concat!(
     env!("CARGO_PKG_VERSION"),
     "+",
@@ -8,1 +8,2 @@
+#[cfg(not(test))]
 pub const CODEX_UNLEASHED_URL: &str = "https://holdmyspot.com/codex-unleashed/";
diff --git a/codex-rs/tui/src/history_cell/session.rs b/codex-rs/tui/src/history_cell/session.rs
--- a/codex-rs/tui/src/history_cell/session.rs
+++ b/codex-rs/tui/src/history_cell/session.rs
@@ -4,1 +4,2 @@
+#[cfg(not(test))]
 use crate::version::CODEX_UNLEASHED_URL;
@@ -323,5 +323,6 @@
             Span::from(format!("(v{})", self.version)).dim(),
         ];
+        #[cfg(not(test))]
         let provider_url = Line::from(vec![
             Span::from("   "),
             Span::from(CODEX_UNLEASHED_URL).cyan().underlined(),
@@ -376,3 +376,2 @@
             make_row(title_spans),
-            provider_url,
             make_row(Vec::new()),
@@ -381,4 +380,6 @@
             make_row(dir_spans),
         ];
+        #[cfg(not(test))]
+        lines.insert(1, provider_url);
 
         if self.yolo_mode {
@@ -398,3 +398,2 @@
             Line::from(format!("OpenAI Codex (v{})", self.version)),
-            Line::from(format!("   {CODEX_UNLEASHED_URL}")),
             Line::from(format!(
@@ -411,2 +410,4 @@
         ];
+        #[cfg(not(test))]
+        lines.insert(1, Line::from(format!("   {CODEX_UNLEASHED_URL}")));
         if self.yolo_mode {
diff --git a/codex-rs/core/tests/suite/openai_file_mcp.rs b/codex-rs/core/tests/suite/openai_file_mcp.rs
--- a/codex-rs/core/tests/suite/openai_file_mcp.rs
+++ b/codex-rs/core/tests/suite/openai_file_mcp.rs
@@ -44,5 +44,4 @@
 use wiremock::Mock;
 use wiremock::MockServer;
 use wiremock::ResponseTemplate;
-use wiremock::matchers::body_json;
 use wiremock::matchers::body_partial_json;
