diff --git a/codex-rs/tui/BUILD.bazel b/codex-rs/tui/BUILD.bazel
index 21fb44a..830a83b 100644
--- a/codex-rs/tui/BUILD.bazel
+++ b/codex-rs/tui/BUILD.bazel
@@ -17,7 +17,8 @@ codex_rust_crate(
         "src/**/snapshots/**",
         "tests/fixtures/**",
     ]),
     test_shard_counts = {
         "tui-unit-tests": 8,
     },
+    windows_cross_test_exclude = ["manager_dependency_regression"],
 )
diff --git a/codex-rs/windows-sandbox-rs/src/unified_exec/tests.rs b/codex-rs/windows-sandbox-rs/src/unified_exec/tests.rs
index f843299..384ee5b 100644
--- a/codex-rs/windows-sandbox-rs/src/unified_exec/tests.rs
+++ b/codex-rs/windows-sandbox-rs/src/unified_exec/tests.rs
@@ -677,6 +677,10 @@ fn legacy_capture_emits_output_and_preserves_descendant_after_normal_exit() {
 
 #[test]
 fn legacy_workspace_write_delete_is_limited_to_writable_roots() {
+    if std::env::var_os("GITHUB_ACTIONS").is_some() {
+        eprintln!("skipping capability ACL regression test on hosted GitHub runners");
+        return;
+    }
     let _guard = legacy_process_test_guard();
     let runtime = current_thread_runtime();
     runtime.block_on(async move {
diff --git a/defs.bzl b/defs.bzl
index 48922cd..95309c3 100644
--- a/defs.bzl
+++ b/defs.bzl
@@ -206,6 +206,7 @@ def codex_rust_crate(
         test_data_extra = [],
         test_shard_counts = {},
         test_tags = [],
+        windows_cross_test_exclude = [],
         test_threads = 0,
         unit_test_timeout = None,
         extra_binaries = [],
@@ -681,7 +684,7 @@ def codex_rust_crate(
             runfile_env = integration_test_cargo_env_runfiles,
             test_bin = ":" + windows_cross_test_binary,
             workspace_root_marker = "//codex-rs/utils/cargo-bin:repo_root.marker",
-            target_compatible_with = WINDOWS_GNULLVM_ONLY,
+            target_compatible_with = ["@platforms//:incompatible"] if test_file_stem in windows_cross_test_exclude else WINDOWS_GNULLVM_ONLY,
             tags = test_tags,
             **windows_cross_test_kwargs
         )
