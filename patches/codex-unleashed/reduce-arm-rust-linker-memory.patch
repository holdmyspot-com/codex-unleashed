diff --git a/MODULE.bazel b/MODULE.bazel
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -269,0 +270,15 @@ crate = use_extension("@rules_rs//rs:extensions.bzl", "crate")
+
+crate.annotation(
+    crate = "codex-code-mode-host",
+    rustc_flags_select = {
+        "aarch64-unknown-linux-gnu": ["-C", "link-arg=-Wl,--no-keep-memory"],
+        "aarch64-unknown-linux-musl": ["-C", "link-arg=-Wl,--no-keep-memory"],
+    },
+)
+
+crate.annotation(
+    crate = "codex-code-mode-runtime",
+    rustc_flags_select = {
+        "aarch64-unknown-linux-gnu": ["-C", "link-arg=-Wl,--no-keep-memory"],
+        "aarch64-unknown-linux-musl": ["-C", "link-arg=-Wl,--no-keep-memory"],
+    },
+)
